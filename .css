/* =========================
   ECOSYSTEM TITLE
========================= */
.eco-our {
  color: white;
  font-weight: 800;
}

.eco-main {
  color: #d4af37;
  font-weight: 800;
  font-style: italic;
  font-family: 'Playfair Display', serif;
}

/* =========================
   MEDIA & STORIES
========================= */
.media-title {
  color: white;
  font-weight: 800;
}

.media-stories {
  color: #d4af37;
  font-weight: 800;
  font-style: italic;
  font-family: 'Playfair Display', serif;
}

/* =========================
   OUR PARTNERS
========================= */
.partner-our {
  color: white;
  font-weight: 800;
}

.partner-main {
  color: #d4af37;
  font-weight: 800;
  font-style: italic;
  font-family: 'Playfair Display', serif;
}

/* =========================
   PARTNER WITH KENAME
========================= */
.partner-with {
  color: white;
  font-weight: 800;
}

.partner-kename {
  color: #d4af37;
  font-weight: 800;
  font-style: italic;
  font-family: 'Playfair Display', serif;
}

/* =========================
   SUBMIT INQUIRY
========================= */
.submit-title {
  color: white;
  font-weight: 800;
}
